name: ducktinycc
version: 0.1.0.9000
description: Compile and register C scalar UDFs from SQL via TinyCC — self-contained, no external runtime required
license: MIT
excluded_platforms: wasm_mvp;wasm_eh;wasm_threads;windows_amd64;windows_arm64
